function redirect(path) {
  if (path == "levels") {
    window.location.href = `pages/${path}.html`;
  } else if (path == "book") {
    window.location.href = `pages/${path}.html`;
  } else if (path == "dragon-grow") {
    window.location.href = `pages/${path}.html`;
  }
}
